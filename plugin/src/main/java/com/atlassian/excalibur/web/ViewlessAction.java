package com.atlassian.excalibur.web;

/**
 * A ViewlessAction is one that does work on behlf of other actions but never displays errors
 * and so on but rather produces them
 */
public interface ViewlessAction {
}
