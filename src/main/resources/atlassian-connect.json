{
  "key": "${addon.key}",
  "baseUrl": "${addon.base-url}",
  "name": "Capture Cloud (Spring Boot)",
  "authentication": {
    "type": "jwt"
  },
  "lifecycle": {
    "installed": "/installed",
    "uninstalled": "/uninstalled"
  }
}
